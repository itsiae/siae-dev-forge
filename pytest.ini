[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"' or run with '-m slow')
    integration: integration tests with external tools (semgrep, gh, etc.)
    semgrep: tests requiring semgrep CLI binary installed
