# Task 01 — estimate_size.py: anti-greedy P1 + generated-file skip

**Fix-group:** G3, G4
**ADR riferito:** ADR-3 (anchored path_patterns), ADR-4 (generated marker)
**Stato:** [PENDING]
**Dipendenze:** —

## File modificati

- `skills/code-coverage/scripts/estimate_size.py`
- `skills/code-coverage/scripts/tests/test_estimate_size_anchored.py` (NEW)

## Test (TDD-first)

Crea `test_estimate_size_anchored.py` con questi case (pytest stdlib-only):

1. `test_classify_priority_anchored_to_last_2_segments`:
   - Input: `rel_path="it/siae/pae/services/foo/Bar.java"`, priority_rules con P1 `**/services/**`
   - Atteso: `None` (Pass-1 parent="foo" no match; Pass-2 anchor="foo/Bar.java" no match per `services`)
   - Pre-fix: ritornava `"P1"` (greedy match su segment intermedio)

2. `test_classify_priority_parent_dir_match_still_works`:
   - Input: `rel_path="src/service/payment.ts"`, Pass-1 parent="service" in P1.parent_dirs
   - Atteso: `"P1"` (Pass-1 hit, backward compat)

3. `test_is_generated_file_detects_at_generated_marker`:
   - Crea tmpfile con prima riga `// @generated by openapi-codegen`
   - generated_markers=`["@generated"]`
   - Atteso: `True`

4. `test_is_generated_file_returns_false_for_normal_file`:
   - Crea tmpfile con `export function add(a, b) { return a + b; }`
   - Atteso: `False`

5. `test_is_generated_file_handles_oserror`:
   - Passa `Path("/nonexistent/file.ts")`
   - Atteso: `False` (no raise)

## Implementazione

In `estimate_size.py`:

1. Modifica `_classify_priority` Pass-2: calcola `anchor_window = "/".join(rel_path.split("/")[-2:])`, applica `re.search(regex, anchor_window)` invece di `re.search(regex, rel_path)`.

2. Aggiungi funzione `_is_generated_file(abs_path: Path, generated_markers: list[str]) -> bool`:
   - Sniff prime `_HEADER_SNIFF_BYTES=1024` con `read_bytes()[:1024].decode("utf-8", errors="ignore")`
   - `return any(marker in head for marker in generated_markers)`
   - On `OSError` ritorna `False`

3. In `main()`: dopo aver letto `priority_rules`, estrai `generated_markers = priority_rules.get("generated_file_markers", [])`. Nel walk loop, prima di append a `file_entries`, skip se `_is_generated_file(fpath, generated_markers)` → `total_files -= 1` not needed (skip pre-conteggio).

## Criterio di accettazione

- `pytest skills/code-coverage/scripts/tests/test_estimate_size_anchored.py -v` → 5/5 PASS
- `pytest skills/code-coverage/scripts/tests/` complete → 0 regression
- Re-run su `itsiae/pae-pae-services-be` clone: P1 file count cala da 100/111 a ≤45/111
