# Task 02 — Add "When to use" + "Supported stacks" sections

**Stato**: [COMPLETED] · **Effort**: 10min · **File toccati**: 1

## Goal

Aggiungere a `SKILL.md` due sezioni mancanti rispetto alla best practice
Anthropic:

- `## When to use` simmetrica all'esistente `## When NOT to use`, con
  trigger condition esplicite per discoverability.
- `## Supported stacks` che recupera l'elenco stack rimosso dalla
  description in task-01.

## Acceptance

- Sezione `## When to use` esiste, ≤8 righe, lista trigger concreti.
- Sezione `## Supported stacks` esiste, elenca Tier-1 + Tier-2 con
  rinvio a `references/stacks/INDEX.md`.
- Posizione: tra l'intro e `## How to invoke`.

## Implementation

Edit `SKILL.md`, inserzione di 2 sezioni prima di `## How to invoke`.
