{
  "entry_points": [
    {
      "id": "orders.list",
      "boundary_id": "GET /v1/orders",
      "kind": "http-route",
      "unit_id": "orders-api",
      "trigger": "GET /v1/orders?cursor=<ts>",
      "actor": "api-caller",
      "inputs": [
        {"name": "cursor", "type": "ISO-8601 timestamp"},
        {"name": "limit", "type": "integer (default 20)"}
      ],
      "downstream_calls": [
        "SELECT id, created_at, total FROM orders WHERE created_at < $cursor ORDER BY created_at DESC LIMIT $limit"
      ],
      "side_effects": [],
      "source_ref": {
        "file": "orders/repository.py",
        "line_start": 12,
        "line_end": 30,
        "sha": "c3d4e5f",
        "dirty_flag": false
      }
    }
  ],
  "stack_context": "python+sqlalchemy+postgres; orders table has primary key id (bigserial) and indexed created_at; cursor is only created_at, no id tie-breaker"
}
